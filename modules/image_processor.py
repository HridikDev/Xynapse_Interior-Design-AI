from PIL import Image

def segment_room(image: Image.Image) -> Image.Image:
    # Placeholder - return original image
    return image

def generate_stylized_image(image: Image.Image, prompt: str) -> Image.Image:
    # Simulate generation (replace with real API call or local model)
    return image
